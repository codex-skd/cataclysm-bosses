<h2>v0.0.0-beta.3 - Fix startup crash</h2>

<p><strong>v0.0.0-beta.2 crashed on startup</strong> with <code>IllegalArgumentException: class ... has no @SubscribeEvent methods, but register was called anyway</code>. Fixed.</p>

<h3>Fix</h3>
<ul>
<li>Removed a premature event bus registration call in the placeholder main mod class — it had no listener methods yet, which this NeoForge version now rejects outright instead of silently ignoring.</li>
</ul>

<h3>Status</h3>
<ul>
<li><strong>Confirmed loading correctly</strong> alongside other mods in a live client session. Still no ported content — scaffolding only.</li>
</ul>
