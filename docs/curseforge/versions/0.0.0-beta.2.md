<h2>v0.0.0-beta.2 - Fix invalid mod file</h2>

<p><strong>v0.0.0-beta.1 failed to load</strong> ("not a valid mod file") due to a build configuration bug — the mod's metadata file never made it into the jar correctly. This release fixes that; the mod now loads.</p>

<h3>Fix</h3>
<ul>
<li>Corrected the build so <code>META-INF/neoforge.mods.toml</code> is generated and packaged properly.</li>
<li>Mod ID normalized to <code>the_sundering</code>.</li>
</ul>

<h3>Status</h3>
<ul>
<li><strong>Still project scaffolding only</strong> — no ported content yet. This release just confirms the mod loads correctly in-game.</li>
</ul>
